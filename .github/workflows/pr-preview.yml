name: PR Preview Environment

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
    paths-ignore:
      - '**.md'
      - 'infra/pulumi/**'

permissions:
  contents: read
  packages: write
  pull-requests: write
  issues: write

env:
  REGISTRY: ghcr.io
  API_IMAGE_NAME: ${{ github.repository }}/api
  WEB_IMAGE_NAME: ${{ github.repository }}/web
  DOMAIN_SUFFIX: preview.mydomain.com

jobs:
  # TODO: Uncomment this when we have a way to destroy the preview environment
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Tests
        run: |
          echo "Tests passed"
  # Job to destroy preview environment when PR is closed or merged
  # cleanup:
  #   if: github.event.action == 'closed'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Configure Kubeconfig
  #       run: |
  #         mkdir -p ~/.kube
  #         # Handles both base64 encoded and raw Kubeconfig secrets
  #         if echo "${{ secrets.KUBECONFIG }}" | grep -q '^[A-Za-z0-9+/=]\{4,\}$'; then
  #           echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
  #         else
  #           echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
  #         fi
  #         chmod 600 ~/.kube/config

  #     - name: Delete Kubernetes Namespace
  #       run: |
  #         echo "Deleting namespace: pr-${{ github.event.number }}"
  #         kubectl delete namespace pr-${{ github.event.number }} --wait=false || echo "Namespace already deleted."

  #     - name: Update PR Comment
  #       uses: actions/github-script@v7
  #       with:
  #         script: |
  #           const prNumber = context.issue.number;
  #           const repo = context.repo;

  #           const { data: comments } = await github.rest.issues.listComments({
  #             ...repo,
  #             issue_number: prNumber,
  #           });
            
  #           const stickyComment = comments.find(comment => 
  #             comment.body.includes("PR Preview Environment")
  #           );

  #           if (stickyComment) {
  #             await github.rest.issues.updateComment({
  #               ...repo,
  #               comment_id: stickyComment.id,
  #               body: `🗑️ **PR Preview Environment Destroyed**\n\nThe preview environment for PR #${prNumber} has been successfully torn down. All associated resources and namespaces have been deleted.`
  #             });
  #           }

  # # Job to build, deploy, and verify the preview environment
  # deploy:
  #   if: github.event.action != 'closed'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Set up QEMU
  #       uses: docker/setup-qemu-action@v3

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to GHCR
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Calculate Image Metadata
  #       id: meta
  #       run: |
  #         # Lowercase the image names for registry compliance
  #         API_IMAGE_LOWER=$(echo "${{ env.REGISTRY }}/${{ env.API_IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
  #         WEB_IMAGE_LOWER=$(echo "${{ env.REGISTRY }}/${{ env.WEB_IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
          
  #         # Combine PR number and commit SHA for unique version tagging
  #         TAG="pr-${{ github.event.number }}-${{ github.sha }}"
          
  #         echo "api_image=${API_IMAGE_LOWER}:${TAG}" >> $GITHUB_OUTPUT
  #         echo "web_image=${WEB_IMAGE_LOWER}:${TAG}" >> $GITHUB_OUTPUT
  #         echo "tag=${TAG}" >> $GITHUB_OUTPUT

  #     - name: Build and Push Backend API Image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./apps/api/Dockerfile
  #         push: true
  #         tags: ${{ steps.meta.outputs.api_image }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     - name: Build and Push Frontend Web Image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./apps/web
  #         file: ./apps/web/Dockerfile
  #         push: true
  #         tags: ${{ steps.meta.outputs.web_image }}
  #         build-args: |
  #           NUXT_PUBLIC_API_BASE=https://pr-${{ github.event.number }}.${{ env.DOMAIN_SUFFIX }}/api/v1
  #           NUXT_PUBLIC_WS_BASE=wss://pr-${{ github.event.number }}.${{ env.DOMAIN_SUFFIX }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     - name: Render Kubernetes Manifests
  #       id: render
  #       run: |
  #         # Set environment variables for envsubst
  #         export PR_NUMBER="${{ github.event.number }}"
  #         export COMMIT_SHA="${{ github.sha }}"
  #         export API_IMAGE="${{ steps.meta.outputs.api_image }}"
  #         export WEB_IMAGE="${{ steps.meta.outputs.web_image }}"
  #         export CREATED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
  #         export TTL_EXPIRATION=$(date -u -d "+48 hours" +"%Y-%m-%dT%H:%M:%SZ")

  #         mkdir -p manifests-rendered

  #         for file in infra/kubernetes/preview/*.yaml; do
  #           # Skip cluster-level reaper manifest
  #           if [[ "$file" == *"reaper-cronjob.yaml"* ]]; then
  #             continue
  #           fi
  #           echo "Rendering $file..."
  #           envsubst < "$file" > "manifests-rendered/$(basename $file)"
  #         done
          
  #         echo "ttl_expiration=${TTL_EXPIRATION}" >> $GITHUB_OUTPUT

  #     - name: Configure Kubeconfig
  #       run: |
  #         mkdir -p ~/.kube
  #         if echo "${{ secrets.KUBECONFIG }}" | grep -q '^[A-Za-z0-9+/=]\{4,\}$'; then
  #           echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
  #         else
  #           echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
  #         fi
  #         chmod 600 ~/.kube/config

  #     - name: Deploy to Kubernetes
  #       run: |
  #         # Create namespace and network policy first
  #         kubectl apply -f manifests-rendered/namespace.yaml
  #         kubectl apply -f manifests-rendered/network-policy.yaml
          
  #         # Deploy dependencies (database + cache)
  #         kubectl apply -f manifests-rendered/postgres.yaml
  #         kubectl apply -f manifests-rendered/redis.yaml
          
  #         # Deploy applications and ingress routing
  #         kubectl apply -f manifests-rendered/api.yaml
  #         kubectl apply -f manifests-rendered/web.yaml
  #         kubectl apply -f manifests-rendered/ingress.yaml

  #     - name: Verify Deployments Readiness
  #       run: |
  #         echo "Waiting for core services to start up..."
  #         kubectl rollout status deployment/postgres -n pr-${{ github.event.number }} --timeout=60s
  #         kubectl rollout status deployment/redis -n pr-${{ github.event.number }} --timeout=60s
  #         kubectl rollout status deployment/api -n pr-${{ github.event.number }} --timeout=120s
  #         kubectl rollout status deployment/web -n pr-${{ github.event.number }} --timeout=120s

  #     - name: Create or Update Sticky PR Comment
  #       uses: actions/github-script@v7
  #       env:
  #         TTL_EXPIRATION: ${{ steps.render.outputs.ttl_expiration }}
  #       with:
  #         script: |
  #           const prNumber = context.issue.number;
  #           const repo = context.repo;
  #           const domainSuffix = process.env.DOMAIN_SUFFIX || 'preview.mydomain.com';
  #           const previewUrl = `https://pr-${prNumber}.${domainSuffix}`;
  #           const ttlExpiration = process.env.TTL_EXPIRATION;

  #           const commentBody = `### 🚀 PR Preview Environment Ready!

  #           | Component | Target URL |
  #           |---|---|
  #           | **Frontend UI** | [${previewUrl}](${previewUrl}) |
  #           | **Backend API** | [${previewUrl}/api/v1/health](${previewUrl}/api/v1/health) |

  #           #### Infrastructure Details
  #           - **Namespace:** \`pr-${prNumber}\`
  #           - **Docker Image Tag:** \`${{ steps.meta.outputs.tag }}\`
  #           - **Hard Expiration TTL:** 48 hours (auto-destroy at: \`${ttlExpiration}\`)
  #           - **Cost Optimization Status:** Enabled
  #             - Lightweight namespace-isolated databases (No persistent volume storage fees).
  #             - Nightly auto-scale to zero (active off-hours: 8:00 PM - 8:00 AM in cluster timezone).
  #           `;

  #           // List existing issue/PR comments
  #           const { data: comments } = await github.rest.issues.listComments({
  #             ...repo,
  #             issue_number: prNumber,
  #           });
            
  #           // Find our sticky comment
  #           const stickyComment = comments.find(comment => 
  #             comment.body.includes("PR Preview Environment")
  #           );

  #           if (stickyComment) {
  #             await github.rest.issues.updateComment({
  #               ...repo,
  #               comment_id: stickyComment.id,
  #               body: commentBody,
  #             });
  #             console.log("Updated sticky comment.");
  #           } else {
  #             await github.rest.issues.createComment({
  #               ...repo,
  #               issue_number: prNumber,
  #               body: commentBody,
  #             });
  #             console.log("Created new sticky comment.");
  #           }
