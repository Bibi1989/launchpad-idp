# REST stays same-origin via Nitro/Vite proxy (default /api/v1).
# NUXT_PUBLIC_API_BASE=/api/v1

# Terminal WebSocket — MUST point at the FastAPI process (not the Nuxt port).
NUXT_PUBLIC_WS_BASE=ws://localhost:8000
